[build]
  base = "frontend"
  command = "pnpm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"
  NEXT_PUBLIC_API_URL = "https://domain-market-intelligence-api.onrender.com"
